# Local map file

Put your local HCM City PMTiles file here:

```txt
frontend/public/maps/hcm.pmtiles
```

The frontend is configured to load:

```txt
/maps/hcm.pmtiles
```

If the file is missing, the admin map will show a local map error.
